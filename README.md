# budget-tracker-chrome-extension
A Chrome Extension built using Manifest V3 that allows users to track expenses directly from any webpage using a context menu, with persistent storage and notifications.
